{
  "name": "chitragupt-android",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor Android wrapper for the Chitragupt PWA — loads the app remotely from the Render-hosted server, no bundled web assets.",
  "scripts": {
    "sync": "npx cap sync android",
    "open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
